{
  "name": "verifiai-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix frontend && npm run db:generate --prefix backend",
    "postinstall": "npm install --prefix backend && npm install --prefix frontend && npm run db:generate --prefix backend"
  }
}
